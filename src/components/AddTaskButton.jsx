// Add an input element that creates new todo items and adds them to the list.
// New todos should be added to the top of the list visually; the oldest todos should be at the bottom.
// I think this will use shift so that tasks are placed first on the list 